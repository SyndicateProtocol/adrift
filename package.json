{
  "name": "adrift",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "bun run --filter 'adrift-*' dev",
    "format": "bunx @biomejs/biome format --write .",
    "check": "bunx @biomejs/biome check .",
    "check-write": "bunx @biomejs/biome check --fix .",
    "cron": "bun run --filter 'adrift-cron' cron"
  },
  "dependencies": {
    "@farcaster/miniapp-wagmi-connector": "^1.0.0",
    "@vercel/analytics": "^1.5.0"
  }
}
